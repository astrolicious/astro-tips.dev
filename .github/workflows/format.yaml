name: Format

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  prettier:
    # if: github.repository_owner == 'astrolicious'
    uses: ../automation/format.yml@main
    with:
      command: "format"
    secrets: inherit
